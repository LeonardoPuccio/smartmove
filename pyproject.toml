[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smartmove"
version = "0.3.0"
description = "Cross-filesystem file mover with hardlink preservation"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Leonardo Puccio", email = "me@leonardopuccio.dev"}
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: System Administrators",
    "Topic :: System :: Filesystems",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
    "black>=25.0",
    "isort>=6.0",
    "pre-commit>=4.0",
    "pytest>=8.0",
    "pytest-cov>=4.0",
    "ruff>=0.13",
]

[project.urls]
Homepage = "https://github.com/LeonardoPuccio/smartmove"

[project.scripts]
smv = "smartmove.cli:main"

[tool.setuptools]
packages = ["smartmove", "smartmove.core", "smartmove.utils"]

[tool.black]
target-version = ["py310", "py311", "py312"]

[tool.coverage.run]
omit = ["tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if __name__ == .__main__.:"
]